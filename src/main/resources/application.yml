server:
  port: 8080

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/moyeorak_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    url: jdbc:mysql://content-db.c92y0sa8e7t2.ap-northeast-2.rds.amazonaws.com:3306/content_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: choi
    password: 1q2w3e4r!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

cloud:
  aws:
    region:
      static: ap-northeast-2   # 서울 리전
    s3:
      bucket: goorm-presigned-url
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

